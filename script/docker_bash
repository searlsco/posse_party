#!/usr/bin/env bash
set -euo pipefail

compose_file="docker-compose.yml"

if [[ ! -f "${compose_file}" ]]; then
  echo "docker-compose.yml is missing. Re-run setup.sh on this host." >&2
  exit 1
fi

docker compose -f "${compose_file}" exec -it web bash
